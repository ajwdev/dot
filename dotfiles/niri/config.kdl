// niri config for tomservo — ported from ../hypr/hyprland.conf.
// Trialing niri's scrolling tiling against Hyprland. KDL reference:
// https://github.com/YaLTeR/niri/wiki/Configuration:-Overview
//
// NOTE: niri rejects the entire config on an unknown node, so this sticks to
// well-established nodes. Hyprland-only concepts that don't map (dwindle/master
// layouts, per-window idle-inhibit, pseudo/togglesplit) are intentionally
// dropped — niri is a single scrolling-column layout.

/////////////
// INPUT
/////////////

input {
    keyboard {
        xkb {
            layout "us"
            // Caps Lock as Shift-escape, Alt and Win swapped (matches hyprland).
            options "caps:escape_shifted_capslock,altwin:swap_alt_win"
        }
        numlock
    }

    touchpad {
        tap
        natural-scroll
    }

    mouse {
        natural-scroll
    }

    // hyprland: follow_mouse = 1
    focus-follows-mouse
}

/////////////
// OUTPUTS
/////////////
// hyprland:
//   monitor=HDMI-A-1,3840x2160@60,0x0,1
//   monitor=desc:Samsung ... Odyssey G95NC HNTX401001,7680x2160@120,3840x0,1

output "HDMI-A-1" {
    mode "3840x2160@60.000"
    scale 1
    position x=0 y=0
}

output "Samsung Electric Company Odyssey G95NC HNTX401001" {
    mode "7680x2160@120.000"
    scale 1
    position x=3840 y=0
}

/////////////
// LAYOUT
/////////////
// Rough analog to hyprland gaps_in=5 / gaps_out=20 / border_size=2.

layout {
    gaps 12
    border {
        width 2
        active-color "#5599bb"
        inactive-color "#595959"
    }
    shadow {
        on
        softness 30
        spread 5
        offset x=0 y=5
        color "#00000060"
    }
}

animations {
    slowdown 0.8
}

gestures {
    hot-corners {
        off
    }
}

/////////////
// STARTUP
/////////////
// hyprland exec-once: qs, dunst, hyprpaper, hyprpolkitagent, copyq, hypridle.
// niri equivalents (swaybg/swayidle/swaylock replace the hypr* daemons).

// Forked quickshell bar (see ../quickshell-niri). Uses --path so it doesn't
// collide with the Hyprland config symlinked at ~/.config/quickshell.
spawn-at-startup "qs"
spawn-at-startup "dunst"
spawn-at-startup "copyq" "--start-server"
spawn-at-startup "sh" "-c" "systemctl --user start hyprpolkitagent"
spawn-at-startup "xwayland-satellite"
spawn-at-startup "swaybg" "-i" "/home/andrew/Pictures/Wallpapers/wp6654679.png" "-m" "fill"
// Idle → lock (replaces hypridle/hyprlock).
spawn-at-startup "swayidle" "-w" \
    "timeout" "600" "hyprlock" \
    "timeout" "900" "niri msg action power-off-monitors" \
    "resume" "niri msg action power-on-monitors" \
    "before-sleep" "hyprlock"

// niri needs to know xwayland-satellite's display for X11 clients.
environment {
    DISPLAY ":0"
}

prefer-no-csd

/////////////
// KEYBINDS
/////////////
// Mod = Super (physical Win; with altwin:swap_alt_win the physical Alt).

// Rebased on niri's default binds (vim keys, overview, Ctrl-to-move,
// column consume/expel), with a few kept-from-hyprland habits marked [hypr].
binds {
    Mod+Shift+Slash { show-hotkey-overlay; }

    // Programs. [hypr] ghostty terminal + Alt+Space launcher.
    Mod+T hotkey-overlay-title="Terminal: ghostty" { spawn "ghostty"; }
    Mod+D hotkey-overlay-title="Launcher: fuzzel" { spawn "fuzzel"; }
    Alt+Space hotkey-overlay-title="Launcher: fuzzel" { spawn "fuzzel"; } // [hypr]
    Super+Alt+L hotkey-overlay-title="Lock: hyprlock" { spawn "hyprlock"; }

    // Overview — niri's zoomed-out workspace/window view.
    Mod+O repeat=false { toggle-overview; }
    Mod+Q repeat=false { close-window; }

    // Focus (arrows + vim keys).
    Mod+Left  { focus-column-left; }
    Mod+Right { focus-column-right; }
    Mod+Up    { focus-window-up; }
    Mod+Down  { focus-window-down; }
    Mod+H { focus-column-left; }
    Mod+L { focus-column-right; }
    Mod+J { focus-window-down; }
    Mod+K { focus-window-up; }
    Mod+Home { focus-column-first; }
    Mod+End  { focus-column-last; }

    // Move window/column (Ctrl = move).
    Mod+Ctrl+Left  { move-column-left; }
    Mod+Ctrl+Right { move-column-right; }
    Mod+Ctrl+Up    { move-window-up; }
    Mod+Ctrl+Down  { move-window-down; }
    Mod+Ctrl+H { move-column-left; }
    Mod+Ctrl+L { move-column-right; }
    Mod+Ctrl+J { move-window-down; }
    Mod+Ctrl+K { move-window-up; }
    Mod+Ctrl+Home { move-column-to-first; }
    Mod+Ctrl+End  { move-column-to-last; }

    // Monitors (Shift = monitor).
    Mod+Shift+Left  { focus-monitor-left; }
    Mod+Shift+Right { focus-monitor-right; }
    Mod+Shift+Up    { focus-monitor-up; }
    Mod+Shift+Down  { focus-monitor-down; }
    Mod+Shift+H { focus-monitor-left; }
    Mod+Shift+L { focus-monitor-right; }
    Mod+Shift+J { focus-monitor-down; }
    Mod+Shift+K { focus-monitor-up; }
    Mod+Shift+Ctrl+Left  { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+Right { move-column-to-monitor-right; }
    Mod+Shift+Ctrl+Up    { move-column-to-monitor-up; }
    Mod+Shift+Ctrl+Down  { move-column-to-monitor-down; }

    // Workspaces.
    Mod+Page_Down { focus-workspace-down; }
    Mod+Page_Up   { focus-workspace-up; }
    Mod+U { focus-workspace-down; }
    Mod+I { focus-workspace-up; }
    Mod+Ctrl+Page_Down { move-column-to-workspace-down; }
    Mod+Ctrl+Page_Up   { move-column-to-workspace-up; }
    Mod+Ctrl+U { move-column-to-workspace-down; }
    Mod+Ctrl+I { move-column-to-workspace-up; }
    Mod+Shift+Page_Down { move-workspace-down; }
    Mod+Shift+Page_Up   { move-workspace-up; }

    // Mouse wheel over workspaces / columns.
    Mod+WheelScrollDown      cooldown-ms=150 { focus-workspace-down; }
    Mod+WheelScrollUp        cooldown-ms=150 { focus-workspace-up; }
    Mod+Ctrl+WheelScrollDown cooldown-ms=150 { move-column-to-workspace-down; }
    Mod+Ctrl+WheelScrollUp   cooldown-ms=150 { move-column-to-workspace-up; }
    Mod+WheelScrollRight      { focus-column-right; }
    Mod+WheelScrollLeft       { focus-column-left; }
    Mod+Ctrl+WheelScrollRight { move-column-right; }
    Mod+Ctrl+WheelScrollLeft  { move-column-left; }

    // Workspaces by index.
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }
    Mod+Ctrl+1 { move-column-to-workspace 1; }
    Mod+Ctrl+2 { move-column-to-workspace 2; }
    Mod+Ctrl+3 { move-column-to-workspace 3; }
    Mod+Ctrl+4 { move-column-to-workspace 4; }
    Mod+Ctrl+5 { move-column-to-workspace 5; }
    Mod+Ctrl+6 { move-column-to-workspace 6; }
    Mod+Ctrl+7 { move-column-to-workspace 7; }
    Mod+Ctrl+8 { move-column-to-workspace 8; }
    Mod+Ctrl+9 { move-column-to-workspace 9; }

    // Column / window management (the heart of scrolling tiling).
    Mod+BracketLeft  { consume-or-expel-window-left; }
    Mod+BracketRight { consume-or-expel-window-right; }
    Mod+Comma  { consume-window-into-column; }
    Mod+Period { expel-window-from-column; }
    Mod+W { toggle-column-tabbed-display; }
    Mod+R { switch-preset-column-width; }
    Mod+Shift+R { switch-preset-window-height; }
    Mod+Ctrl+R { reset-window-height; }
    Mod+F { maximize-column; }
    Mod+Shift+F { fullscreen-window; }
    Mod+Ctrl+F { expand-column-to-available-width; }
    Mod+C { center-column; }
    Mod+Ctrl+C { center-visible-columns; }
    Mod+Minus { set-column-width "-10%"; }
    Mod+Equal { set-column-width "+10%"; }
    Mod+Shift+Minus { set-window-height "-10%"; }
    Mod+Shift+Equal { set-window-height "+10%"; }
    Mod+V       { toggle-window-floating; }
    Mod+Shift+V { switch-focus-between-floating-and-tiling; }

    // Screenshots — niri built-in.
    Print { screenshot; }
    Ctrl+Print { screenshot-screen; }
    Alt+Print { screenshot-window; }
    // [hypr] macOS-style: Super+Shift+3/4/5 save/annotate, Ctrl+Shift+3/4/5 copy.
    Mod+Shift+3 { spawn-sh "grim ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S).png && notify-send 'Screenshot saved' 'Saved to ~/Pictures'"; }
    Mod+Shift+4 { spawn-sh "grim -g \"$(slurp)\" - | swappy -f -"; }
    Mod+Shift+5 { spawn-sh "grim -g \"$(slurp -w)\" - | swappy -f -"; }
    Ctrl+Shift+3 { spawn-sh "grim - | wl-copy && notify-send 'Screenshot copied' 'Copied to clipboard'"; }
    Ctrl+Shift+4 { spawn-sh "grim -g \"$(slurp)\" - | wl-copy && notify-send 'Screenshot copied' 'Copied to clipboard'"; }
    Ctrl+Shift+5 { spawn-sh "grim -g \"$(slurp -w)\" - | wl-copy && notify-send 'Screenshot copied' 'Copied to clipboard'"; }

    // Volume / mic / brightness (allow-when-locked).
    XF86AudioRaiseVolume allow-when-locked=true { spawn-sh "wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%+"; }
    XF86AudioLowerVolume allow-when-locked=true { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-"; }
    XF86AudioMute        allow-when-locked=true { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"; }
    XF86AudioMicMute     allow-when-locked=true { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle"; }
    XF86MonBrightnessUp   allow-when-locked=true { spawn "brightnessctl" "-e4" "-n2" "set" "5%+"; }
    XF86MonBrightnessDown allow-when-locked=true { spawn "brightnessctl" "-e4" "-n2" "set" "5%-"; }

    // Media keys (playerctl).
    XF86AudioNext  allow-when-locked=true { spawn "playerctl" "next"; }
    XF86AudioPause allow-when-locked=true { spawn "playerctl" "play-pause"; }
    XF86AudioPlay  allow-when-locked=true { spawn "playerctl" "play-pause"; }
    XF86AudioPrev  allow-when-locked=true { spawn "playerctl" "previous"; }

    // Session.
    Mod+Escape allow-inhibiting=false { toggle-keyboard-shortcuts-inhibit; }
    Mod+Shift+E { quit; }
    Ctrl+Alt+Delete { quit; }
    Mod+Shift+P { power-off-monitors; }
}

/////////////
// WINDOW RULES
/////////////
// Ported float rules from hyprland. niri has no per-window center/idle-inhibit,
// so those aspects are dropped.

// Rounded corners for all windows (hyprland: decoration { rounding = 10 }).
// clip-to-geometry rounds the window content, not just the border.
window-rule {
    geometry-corner-radius 10
    clip-to-geometry true
}

// Fallout: New Vegas
window-rule {
    match app-id="steam_app_22380"
    open-floating true
}

// Diablo II: Resurrected
window-rule {
    match app-id="steam_app_2536520"
    open-floating true
}

// waydroid
window-rule {
    match app-id="waydroid.+"
    open-floating true
}

// Picture-in-Picture (Firefox / Brave)
window-rule {
    match title="Picture-in-Picture"
    open-floating true
}
window-rule {
    match title="Picture in picture"
    open-floating true
}
